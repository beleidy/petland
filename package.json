{
  "name": "petland",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "firebase": "^5.9.0",
    "firebaseui": "^3.5.2",
    "moment": "^2.24.0",
    "muicss": "^0.9.9",
    "react": "^16.8.4",
    "react-dom": "^16.8.4",
    "react-router-dom": "^4.3.1"
  },
  "devDependencies": {
    "react-scripts": "^2.1.8"
  },
  "scripts": {
    "start": "firebase use staging && firebase setup:web --json > src/firebase-js-config.json && react-scripts start",
    "build": "firebase setup:web --json > src/firebase-js-config.json && react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
